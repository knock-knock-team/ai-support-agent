# 🚀 AI Support Agent - Quick Start

Полная система для автоматической обработки обращений технической поддержки с помощью AI и веб-интерфейсом для операторов.

## 📦 Что включено

1. **Email Service** - Получение и обработка писем из почтового ящика
2. **Kafka** - Передача сообщений между сервисами
3. **Operator Service** - Веб-интерфейс для операторов и администраторов
4. **AI Agent** - Обработка запросов с помощью LLM (запускается отдельно)

---

## ⚡ Быстрый запуск всей системы

### 1. Предварительные требования

- Docker и Docker Compose
- Python 3.10+ (для AI агента)

### 2. Настройка окружения

```bash
# Создайте .env файл
cp .env.example .env

# Отредактируйте .env и укажите настройки почты
nano .env
```

**Обязательные параметры для email-service:**
```env
MAIL_HOST=imap.gmail.com
MAIL_PORT=993
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
```

### 3. Запуск всех сервисов

```bash
# Из корневой директории проекта
docker-compose up --build -d
```

Это запустит:
- ✅ Zookeeper (порт 2181)
- ✅ Kafka (порты 9092, 29092)
- ✅ PostgreSQL для email-service (порт 5432)
- ✅ Email Processor (обработка писем)
- ✅ PostgreSQL для operator-service (порт 5433)
- ✅ Operator Backend API (порт 8080)
- ✅ Operator Frontend (порт 3000)

### 4. Проверка статуса

```bash
docker-compose ps
```

Все контейнеры должны быть в статусе `Up`.

### 5. Доступ к системе

**Operator Frontend**: http://localhost:3000

**Данные для входа:**
- Логин: `admin`
- Пароль: `Admin123!`

⚠️ **При первом входе система потребует сменить пароль!**

---

## 🔧 Запуск отдельных компонентов

### Только Email Service

```bash
cd email-service
docker-compose up --build -d
```

### Только Operator Service

```bash
cd operator-service
docker-compose up --build -d
```

### AI Agent (Python)

```bash
cd agent

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Запустите агента
python sentiment_classification.py
python text_generation.py
```

---

## 📊 Архитектура

```
┌─────────────────┐
│  Email Server   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐      ┌─────────────┐
│ Email Processor │─────▶│    Kafka    │
└─────────────────┘      └──────┬──────┘
                                │
                                ▼
                         ┌─────────────┐
                         │  AI Agent   │
                         └──────┬──────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │  Operator Service     │
                    │  (Backend + Frontend) │
                    └───────────────────────┘
                                │
                                ▼
                         ┌─────────────┐
                         │  Operators  │
                         └─────────────┘
```

---

## 🎯 Типичный workflow

1. **Письмо поступает** на support@example.com
2. **Email Processor** извлекает письмо и публикует в Kafka топик
3. **AI Agent** обрабатывает запрос:
   - Классифицирует обращение
   - Ищет ответ в базе знаний (RAG)
   - Генерирует ответ
   - Оценивает уверенность
4. Если уверенность **< 70%**:
   - Запрос отправляется оператору через Operator Service
   - Оператор проверяет и корректирует ответ
5. Если уверенность **≥ 70%**:
   - Автоматическая отправка ответа клиенту

---

## 📋 Управление пользователями

### Войдите как администратор

1. Откройте http://localhost:3000
2. Войдите: `admin` / `Admin123!`
3. Смените пароль

### Создайте оператора

1. Перейдите в **Admin Panel**
2. Нажмите **"Create User"**
3. Заполните:
   - Username: `operator1`
   - Password: `TempPass123!`
   - Full Name: `John Operator`
   - Role: **Operator**
4. Нажмите **"Create"**

---

## 🔍 Мониторинг и логи

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f operator-backend
docker-compose logs -f mail-processor
docker-compose logs -f kafka
```

### Проверка Kafka топиков

```bash
docker exec -it kafka kafka-topics --list --bootstrap-server localhost:9092
docker exec -it kafka kafka-console-consumer --topic email-requests --bootstrap-server localhost:9092 --from-beginning
```

### Подключение к БД

```bash
# Email Service DB
docker exec -it postgres-email psql -U postgres -d mail_processor

# Operator Service DB
docker exec -it operator-db psql -U postgres -d operator_service
```

---

## 🛑 Остановка системы

```bash
# Остановить все сервисы
docker-compose down

# Остановить и удалить все данные (включая БД)
docker-compose down -v
```

---

## 🐛 Troubleshooting

### Email Processor не получает письма

1. Проверьте настройки почты в `.env`
2. Для Gmail используйте App Password, а не обычный пароль
3. Проверьте логи: `docker-compose logs mail-processor`

### Operator Frontend не подключается к Backend

1. Убедитесь что backend запущен: `curl http://localhost:8080/api/auth/me`
2. Проверьте логи backend: `docker-compose logs operator-backend`
3. Проверьте что frontend настроен на правильный URL (должен быть `http://localhost:8080`)

### Kafka не стартует

1. Убедитесь что Zookeeper запущен: `docker-compose ps zookeeper`
2. Перезапустите Kafka: `docker-compose restart kafka`

### Порты заняты

Измените порты в `docker-compose.yml`:
- Frontend: `3000:80` → `3001:80`
- Backend: `8080:8080` → `8081:8080`
- Postgres Email: `5432:5432` → `5434:5432`
- Postgres Operator: `5433:5432` → `5435:5432`

---

## 📚 Дополнительная документация

- [Полное описание проекта](README.md)
- [Operator Service документация](operator-service/README.md)
- [API тестирование](operator-service/API_TESTING.md)
- [Учетные данные](operator-service/CREDENTIALS.md)

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs`
2. Проверьте статус контейнеров: `docker-compose ps`
3. Перезапустите проблемный сервис: `docker-compose restart <service-name>`
